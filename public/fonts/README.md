# Fonts

Public site type is **Degular** (OH no Type Company), self-hosted via `next/font/local`.

| Role | Family | Files |
| --- | --- | --- |
| Body / UI | Degular Text | `src/assets/fonts/degular/DegularTextDemo-*.otf` |
| Headlines (`.font-display`) | Degular Display | `src/assets/fonts/degular/DegularDisplayDemo-*.otf` |

Wired in `src/assets/fonts/index.ts` and applied from `src/app/layout.tsx`.
Weights in use: 400 / 500 / 600 / 700.
